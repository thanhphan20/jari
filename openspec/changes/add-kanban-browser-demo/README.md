# add-kanban-browser-demo

Phase 3: make the frontend a working browser demo — log in against the real identity service, and render a real Kanban board from real task data through the gateway.

One screen, deliberately. This is the first change where the backend's behavior becomes visible without `curl`, which is the whole point of it.
